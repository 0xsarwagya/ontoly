# Semantics Validation

Semantics validation checks that the derived artifact matches the current Software Graph hash and only references existing graph nodes.

Run:

```bash
ontoly semantics validate --ci
```

Validation status is based on measured artifact compatibility, not subjective quality scoring.
